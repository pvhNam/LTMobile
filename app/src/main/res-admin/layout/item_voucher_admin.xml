<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:padding="14dp"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="4dp"
    android:elevation="1dp">

    <!-- Header: tên + trạng thái -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="6dp">

        <TextView
            android:id="@+id/tv_voucher_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Tên voucher"
            android:textColor="#212121"
            android:textSize="15sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/tv_voucher_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="10dp"
            android:paddingVertical="3dp"
            android:text="Đang mở"
            android:textSize="11sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Mức giảm -->
    <TextView
        android:id="@+id/tv_voucher_discount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Giảm 10%"
        android:textColor="@color/admin_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginBottom="4dp" />

    <!-- Mô tả -->
    <TextView
        android:id="@+id/tv_voucher_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#616161"
        android:textSize="13sp"
        android:layout_marginBottom="4dp"
        android:visibility="gone" />

    <!-- Thông tin phụ -->
    <TextView
        android:id="@+id/tv_voucher_meta"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0 điểm · Không giới hạn"
        android:textColor="#9E9E9E"
        android:textSize="12sp"
        android:layout_marginBottom="10dp" />

    <!-- Nút thao tác -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_voucher_edit"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:layout_marginEnd="6dp"
            android:text="Sửa"
            android:textSize="13sp"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/admin_primary" />

        <Button
            android:id="@+id/btn_voucher_toggle"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:text="Tạm tắt"
            android:textSize="13sp"
            android:textColor="@color/admin_primary"
            android:backgroundTint="@color/role_admin_bg" />

        <Button
            android:id="@+id/btn_voucher_delete"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:text="Xoá"
            android:textSize="13sp"
            android:textColor="@android:color/white"
            android:backgroundTint="#D32F2F" />
    </LinearLayout>

</LinearLayout>
